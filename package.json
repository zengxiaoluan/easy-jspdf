{
  "name": "easy-jspdf",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter easy-jspdf dev",
    "dev:plugin": "pnpm --filter @easy-jspdf/vite-plugin-deps-tree dev",
    "dev:playground": "pnpm --filter @easy-jspdf/playground dev",
    "build": "pnpm --filter easy-jspdf build",
    "build:plugin": "pnpm --filter @easy-jspdf/vite-plugin-deps-tree build",
    "build:all": "pnpm -r build",
    "build:watch": "pnpm -r build:watch",
    "preview": "pnpm --filter easy-jspdf preview",
    "dev:all": "pnpm -r dev",
    "test:all": "pnpm -r test",
    "install:all": "pnpm install",
    "publish:all": "pnpm --recursive publish",
    "catalog:install": "catalog install"
  },
  "devDependencies": {
    "typescript": "~5.8.3"
  },
  "workspaces": [
    "packages/*"
  ]
}